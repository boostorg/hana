<% exec = (1..50).step(2).to_a %>

{
  "title": {
    "text": "Runtime behavior of transform"
  },
  "series": [
    {
      "name": "Homogeneous hana::tuple",
      "data": <%= time_execution('transform/execute.hana.tuple.erb.cpp', exec) %>
    }, {
      "name": "Homogeneous fusion::vector",
      "data": <%= time_execution('transform/execute.fusion.vector.erb.cpp', exec) %>
    }
  ]
}